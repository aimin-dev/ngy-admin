import { NaaResourceLinkPipe } from './naa-resource-link.pipe';

describe('NaaResourceLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new NaaResourceLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
