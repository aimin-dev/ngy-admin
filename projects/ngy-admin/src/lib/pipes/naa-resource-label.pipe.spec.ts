import { NaaResourceLabelPipe } from './naa-resource-label.pipe';

describe('NaaResourceLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new NaaResourceLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
