import { NaaHostDirective } from './naa-host.directive';

describe('NaaHostDirective', () => {
  it('should create an instance', () => {
    const directive = new NaaHostDirective();
    expect(directive).toBeTruthy();
  });
});
